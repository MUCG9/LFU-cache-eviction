#ifndef BAZA_H
#define BAZA_H

#include <stdio.h>
#include <stdlib.h>

#endif


#ifndef STRUCT_H
#define STRUCT_H

#include "struct.h"

#endif

CacheNode* createNode(int key, int value);

Cache* createCache(int capacity);